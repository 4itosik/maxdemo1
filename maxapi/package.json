{
  "name": "max-bot-api-typespec",
  "version": "0.0.32",
  "private": true,
  "description": "TypeSpec-описание Max Bot API (https://dev.max.ru/docs-api)",
  "scripts": {
    "test": "npm run build && npm run validate && python3 scripts/compare.py",
    "build": "tsp compile . && python3 scripts/seal_additional_properties.py && python3 scripts/fill_inherited_stubs.py",
    "validate": "python3 scripts/validate_kb.py -q"
  },
  "devDependencies": {
    "@typespec/compiler": "^1.13.0",
    "@typespec/http": "^1.13.0",
    "@typespec/openapi": "^1.13.0",
    "@typespec/openapi3": "^1.13.0"
  }
}
